{
  "identifier": "default",
  "description": "Default capabilities",
  "local": true,
  "windows": ["main"],
  "permissions": [
    "shell:allow-open",
    "core:default",
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        { "name": "bin/engine", "sidecar": true }
      ]
    }
  ]
}

